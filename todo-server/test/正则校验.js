 // 1. 检查用户名是否符合规则：只能由字母、数字和下划线组成，且必须由字母或数字开头，长度最少1位最多10位
const reg=/^[a-z|A-Z]([a-z|A-Z|_|0-9]){0,9}$/
let str='bb1111111111111111'
// console.log(reg.test(str));

// 2. 检查密码是否符合规则：只能由英文字母、数字和键盘上可见的半角特殊字符组成，长度最少8位最多20位（密码过长服务器一直计算）
const reg2=/^[a-zA-Z]$/
let str2='B'
console.log(reg2.test(str2));